name: Release

# Tags and publishes a GitHub Release when a PR merges into main with
# one of the release:major / release:minor / release:patch labels.
# No matching label -> no release; this is opt-in per PR, not automatic
# on every merge.
#
# The release is created here with GitHub's generated notes, then
# GoReleaser attaches the cross-compiled archives and checksums.txt to
# it (release.mode: append in .goreleaser.yaml). Both steps live in one
# job on purpose: a tag pushed with GITHUB_TOKEN does not trigger other
# workflows, so splitting the build out into a tag-triggered workflow
# would need a PAT and buy nothing.
#
# Version is computed from the latest existing git tag (defaulting to
# v0.0.0 if none exist) plus the label's semver bump. A tag with major
# version 0 is published as a GitHub prerelease — standard semver
# meaning (0.x.y = not yet stable) rather than a flag to remember to
# flip; it stops automatically once a release:major PR bumps to
# v1.0.0.

on:
  pull_request:
    types: [closed]
    branches: [main]

permissions:
  contents: write

jobs:
  release:
    if: >
      github.event.pull_request.merged == true &&
      (
        contains(github.event.pull_request.labels.*.name, 'release:major') ||
        contains(github.event.pull_request.labels.*.name, 'release:minor') ||
        contains(github.event.pull_request.labels.*.name, 'release:patch')
      )
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Determine bump type
        id: bump
        run: |
          if [[ "${{ contains(github.event.pull_request.labels.*.name, 'release:major') }}" == "true" ]]; then
            echo "type=major" >> "$GITHUB_OUTPUT"
          elif [[ "${{ contains(github.event.pull_request.labels.*.name, 'release:minor') }}" == "true" ]]; then
            echo "type=minor" >> "$GITHUB_OUTPUT"
          else
            echo "type=patch" >> "$GITHUB_OUTPUT"
          fi

      - name: Compute next version
        id: version
        run: |
          latest="$(git tag --list 'v*' --sort=-v:refname | head -n1)"
          latest="${latest:-v0.0.0}"
          latest="${latest#v}"
          IFS='.' read -r major minor patch <<< "$latest"

          case "${{ steps.bump.outputs.type }}" in
            major) major=$((major + 1)); minor=0; patch=0 ;;
            minor) minor=$((minor + 1)); patch=0 ;;
            patch) patch=$((patch + 1)) ;;
          esac

          next="v${major}.${minor}.${patch}"
          echo "next=$next" >> "$GITHUB_OUTPUT"

          if [[ "$major" == "0" ]]; then
            echo "prerelease=true" >> "$GITHUB_OUTPUT"
          else
            echo "prerelease=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Tag and create release
        env:
          GH_TOKEN: ${{ github.token }}
          NEXT: ${{ steps.version.outputs.next }}
          PRERELEASE: ${{ steps.version.outputs.prerelease }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git tag -a "$NEXT" -m "$NEXT"
          git push origin "$NEXT"

          if [[ "$PRERELEASE" == "true" ]]; then
            gh release create "$NEXT" --title "$NEXT" --generate-notes --prerelease
          else
            gh release create "$NEXT" --title "$NEXT" --generate-notes
          fi

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      # Cross-compiles every target and uploads the archives and
      # checksums.txt onto the release created above. The tag has to
      # exist and be checked out before this runs, and it is: the step
      # above created it and left HEAD on it.
      - name: Build and upload release artifacts
        uses: goreleaser/goreleaser-action@v6
        with:
          version: "~> v2"
          args: release --clean
        env:
          GITHUB_TOKEN: ${{ github.token }}
